import { CONST } from '../../constants';

function SurveyFooter() {
  return (
    <div
      style={{
        height: CONST.FOOTERHEIGHT,
        width: '100%',
        color: '#FFF',
        display: 'flex',
        alignItems: 'center',
      }}
    >
      여기는 푸터고 pro-gbm 로고가 들어갈거야
    </div>
  );
}

export default SurveyFooter;
